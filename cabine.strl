module CABIN:
	input SECOND;
	input OPEN_DOOR, CLOSE_DOOR;
	output DOOR_OPENED, DOOR_CLOSED;

	loop			
		await OPEN_DOOR;
		await SECOND;
		await SECOND;
		emit DOOR_OPENED;

		await CLOSE_DOOR;
		await SECOND;
		await SECOND;
		emit DOOR_CLOSED;
	end loop

end module
